apiVersion: 1

datasources:
  # Prometheus datasource — tự động được load khi Grafana start
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
